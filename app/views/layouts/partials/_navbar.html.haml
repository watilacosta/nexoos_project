%nav.navbar.navbar-expand-lg.navbar-dark.bg-main.b-2
  .container-fluid
    = link_to root_path do
      = image_tag('logo.png', class: 'navbar-brand')
    %button.navbar-toggler{"aria-controls" => "navbarText", "aria-expanded" => "false", "aria-label" => "Toggle navigation", "data-target" => "#navbarText", "data-toggle" => "collapse", :type => "button"}
      %span.navbar-toggler-icon
    #navbarText.collapse.navbar-collapse
      %ul.navbar-nav.mr-auto
        %li.nav-item
          = active_link_to root_path,
            active: :exclusive,
            class: 'nav-link' do
            Início
            %span.sr-only (current)
        - if can? :index, Requester
          %li.nav-item
            = active_link_to requesters_path,
              active: :exclusive,
              class: 'nav-link' do
              Solicitantes
      - if user_signed_in?
        %ul.navbar-nav
          %li.nav-item
            = active_link_to 'Sair',
              destroy_user_session_path,
              method: :delete,
              class: 'nav-link'